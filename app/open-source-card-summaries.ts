export type OpenSourceCardSummary = {
  problem: string;
  reasoning: string;
  solution: string;
};

export const openSourceCardSummaries: Record<string, OpenSourceCardSummary> = {
  evo: {
    problem: '非重叠的距离型 RPE 只记录达到阈值的 pose，却没有把 pose 0 作为首个端点；轨迹第一段因此永远缺席，开头累计的位姿误差也不会进入统计。',
    reasoning: '连续区间必须以轨迹起点为第一条边界，但距离累积应从第二个 pose 开始，避免构造起点到自身的无效 pair，并继续保留完整轨迹中的原始索引。',
    solution: '先把 pose 0 放入端点列表，再从 pose 1 累计路程。五个等距 pose 的回归确认 `delta=2 m` 会得到 `(0,2)` 与 `(2,4)` 两个完整区间。',
  },
  'vit-pytorch': {
    problem: '`DecorrelationLoss` 在降低采样比例时索引了 batch 而不是 token：序列长度不变，batch 却可能被重排或重复，损失因此没有作用于预期的 token 子集。',
    reasoning: '采样比例只应改变序列维。任意 layer、batch 等前导维都必须保持原位，并为每组前导坐标独立生成 token 排序，不能依赖固定的二维输入假设。',
    solution: '从完整前导形状生成随机索引，沿最后的 token 索引维截取，再通过 `gather(-2)` 选择 token。固定随机种子的测试将它与显式子集对照，确认损失值和维度都一致。',
  },
  langchain: {
    problem: '`AIMessage.content_blocks` 在 Bedrock Converse 路径中会改写源消息：字符串被替换成块列表，未知块的 `index` 被移除；连续读取因此可能得到不同结果，持久化时也会保存被污染的内容。',
    reasoning: '派生属性应该只观察而不改变对象。字符串转换可以使用局部列表；只有需要提升 `index` 的未知块才需复制，无需为保证只读而深拷贝整条消息。',
    solution: '字符串内容只在局部列表中转换，不再写回 `message.content`；处理未知字典块时先复制对象，再从副本提升 `index`。参数化回归覆盖两类消息、字符串与自定义块，并验证重复读取一致、源内容保持不变。',
  },
  instructor: {
    problem: 'Gemini 请求只浅拷贝最外层参数，嵌套 `safety_settings` 仍与调用方共享；合并默认阈值会原地污染应用配置。复用同一字典发起后续请求时，行为因此取决于它是否曾经过 Instructor。',
    reasoning: '请求规范化必须把调用者输入当作只读。外层字典已复制并不代表嵌套映射也被隔离；只有打断对象别名，才能同时保留默认补全和跨请求状态独立性。',
    solution: '先复制安全设置，再在独立映射中合并默认阈值，并用身份与内容断言锁定输入不变。输出拥有独立的嵌套映射，默认阈值照常生效，复用同一配置的后续请求不再继承前一次调用的副作用。',
  },
  webots: {
    problem: '负单位四元数表示恒等旋转，却会走到零向量归一化，生成 NaN 旋转轴并把崩溃风险带入场景树。这个输入在数学上完全合法，旧实现却把等价表示变成了渲染与仿真的不稳定源。',
    reasoning: 'q 与 −q 描述同一旋转；近零向量应规范化为有限的恒等轴角，同时保留非退化负标量输入的真实旋转。判断边界应基于向量部分的范数，而不是简单检查标量正负。',
    solution: '统一复用向量范数，近零时直接返回规范恒等旋转，其余输入继续正常归一化，并补齐退化边界记录。这样既消除除零，也不改变普通负标量四元数的轴角结果。',
  },
  'robotics-toolbox-python': {
    problem: '`accel_x()` 会引用未定义变量，并把关节数误当作输出列数，让 7 轴机器人无法返回六维末端加速度。正常调用会直接触发 `NameError`，冗余机械臂的结果形状也与接口承诺不符。',
    reasoning: '末端加速度必须同时包含雅可比导数项与关节加速度映射，且输出维度始终属于六维操作空间。关节自由度只决定内部求解规模，不应该改变末端速度与加速度的物理维度。',
    solution: '复用解析雅可比及其导数，恢复 `xdd = J̇a q̇ + Ja q̈`，并统一单点与轨迹输出形状。Puma 560 与 7 轴 Panda 都由同一公式返回稳定的六维结果。',
  },
  simbody: {
    problem: 'OBJ 解析在读取 token 后又查找字面空格，遇到 Tab、换页符等合法空白时会吞掉后续顶点。四边形可能因此被误读成不足四个顶点，纹理与法线索引也会随之错位。',
    reasoning: '格式化流已经完整处理所有空白边界；额外分隔规则不仅重复，还破坏了 `v/vt/vn` 三元组的完整性。真正需要保留的是斜杠索引和续行语义，而不是再建立一套字面空格规则。',
    solution: '移除冗余的空格查找，让格式化提取成为唯一入口，并用混合空白与续行面片锁定行为。回归同时验证顶点、纹理坐标和法线索引，避免只修复表面顶点数量。',
  },
  faiss: {
    problem: '空索引在 BLAS 路径中提前返回，距离与标签缓冲区没有写入哨兵值，可能泄露调用方残留数据。查询规模一旦越过分发阈值，同一个公开接口就会给出与小批次完全不同的结果。',
    reasoning: '空搜索仍是定义明确的接口；不同 result handler 必须完成初始化，而不是跳过整个处理生命周期。哨兵距离和 −1 标签应由各处理器写入，外层不应复制三套结果规则。',
    solution: '将空数据库送入顺序处理路径，覆盖内积、L2 与三类 handler，保证所有槽位写出中性距离和 −1。测试预填非哨兵数据并强制越过原 BLAS 阈值，确认输出不再残留。',
  },
  gtsam: {
    problem: '先写入 value、后到达 factor 的 pending key 没有索引或 clique，过期时却被送进叶节点边缘化并失败。异步到达会把更新带入不存在的 Bayes-tree 路径。',
    reasoning: '尚未连接与已经失效是两种状态；窗口内要等待迟到 factor，真正过期后才能安全回收。当前更新中新到达的 factor 还必须立刻激活对应键，不能被同一轮过期判断误删。',
    solution: '按 active、connected 与 pending 拆分键：已连接者正常边缘化，未连接且过期者直接清理状态。既允许 value 与 factor 异步到达，也避免状态长期泄漏。',
  },
  'jolt-physics': {
    problem: 'GJK 收敛失败后仍保留被拒绝的支撑点，接触重建形成近退化单纯形，并报告数米级假穿透。原本只是轻微接触的圆柱与胶囊，会被错误解释成约 7.25 米的深度重叠。',
    reasoning: '最后的试探点没有改善最近距离，就不属于有效单纯形；接受命中时必须回到加入它之前的几何状态。否则极端重心权重会放大一个已经被算法拒绝的顶点，并污染最终接触位置。',
    solution: '失败返回前弹出无效支撑点，只用最后一个有效单纯形重建接触，并以圆柱—胶囊案例锁定微小误差。修复后穿透深度降到微米量级，同时保持既有 GJK 快速路径。',
  },
  pytorch3d: {
    problem: '视锥剔除把坐标轴当成顶点轴，实际检查的是一个顶点的三个坐标，而非三个顶点是否同在平面外。它会同时制造误剔除和漏剔除，使渲染结果随顶点顺序发生变化。',
    reasoning: '只有三顶点位于同一裁剪平面外才能删除；跨越相机平面的透视三角形还必须避免 XY 镜像误剔除。正交投影不发生这种镜像，Z 轴也需要保留独立判定。',
    solution: '沿坐标维读取并聚合顶点维，再用前方深度掩码约束透视 XY 剔除，补齐六平面与投影回归。4,096 个固定种子三角形与独立 oracle 对照后保持零不一致。',
  },
  rtabmap: {
    problem: '无效检测选项在校验前就创建进度对话框；警告后提前返回，留下无法推进也无法收尾的孤立窗口。用户关闭配置警告后，界面仍残留一个没有任务可执行的进度状态。',
    reasoning: '进度 UI 代表有效任务已经开始，生命周期应由工作路径管理；前置条件失败时根本不该分配它。与其在多个错误出口补清理，不如从控制流上禁止无效资源创建。',
    solution: '把所有校验移到窗口创建之前，非法请求只显示警告，合法任务继续沿用原有进度与取消行为。改动只调整七行代码的位置，不影响正常回环检测的执行路径。',
  },
  symforce: {
    problem: 'Rust codegen 把矩阵压成向量，`2×3` 返回值变成六元素 `SVector`，静态形状语义完全丢失。元素数虽然正确，类型系统却无法区分 `2×3`、`3×2` 与 `6×1`。',
    reasoning: '元素数量相同不代表类型等价；函数签名和返回构造器必须共享同一套 shape-aware 判断。多输出路径已经具备正确逻辑，单返回值不应维护另一套压平规则。',
    solution: '统一使用现有 `format_typename`，让非方阵生成 `SMatrix`，列向量继续保持 `SVector`。两个符号后端都会生成并编译形状正确的 Rust 静态矩阵类型。',
  },
  'lm-evaluation-harness': {
    problem: '延迟注解改变 dataclass 字段的运行时类型，使 CLI 与 YAML 对字典配置出现不同解析。同一任务会因入口不同得到不同对象，偏差还可能在评测开始后才显现。',
    reasoning: '根因不是单个任务配置，而是两条入口缺少共享的类型识别和规范化边界，新增字段还会重复踩坑。类型判断必须兼容延迟注解，配置转换也应只保留一个权威入口。',
    solution: '集中识别字典字段，并把 YAML 字符串规范化前移到公共流水线，让所有入口复用同一解析路径。这样既修复当前字段，也降低后续增加字典参数时再次分叉的维护成本。',
  },
  numpyro: {
    problem: 'JAX 升级后闭包常量进入 jaxpr 输入序列，provenance 的动态参数与变量映射发生整体错位。结果不是立即崩溃，而是来源追踪悄悄关联到错误输入，影响后续推断解释。',
    reasoning: '旧实现依赖私有 tracing API 的常量布局；常量与动态输入必须在稳定公共接口上显式分离。只有先恢复真实输入边界，provenance 才能继续按变量关系传播。',
    solution: '改用 `jax.make_jaxpr` 获取 ClosedJaxpr，拆分常量与动态输入，再重建 provenance 映射。实现摆脱私有 tracing 细节，并为后续 JAX 升级保留稳定接口。',
  },
  opencv: {
    problem: '极端反射坐标依靠逐次修正，不仅会触发整数溢出，还可能执行十亿级循环。输入离图像越远，运行时间越不可控，边界函数甚至可能成为整条视觉流水线的阻塞点。',
    reasoning: '反射边界本质是固定周期映射，无需模拟每一次折返；在宽整数域求周期位置即可直接定位结果。关键是先在完整数值域内归一化，再处理正负方向和端点语义。',
    solution: '将循环改写为 int64 周期模运算，并覆盖负坐标与短边界，把最坏复杂度从 O(N) 降到 O(1)。极端输入不再溢出，普通边界行为和既有接口结果保持一致。',
  },
  deepspeed: {
    problem: 'ZenFlow 接受 `topk_ratio=0/1` 和非正 `update_interval`，训练初始化后会触发除零；小分区还可能因整数截断得到 `topk(k=0)`，让索引与梯度缓冲区失去语义。',
    reasoning: '自动与显式更新必须共享配置边界；选择数、索引缓冲区与梯度缓冲区也应复用同一分区公式。边界检查只有放在训练初始化之前，才能避免模型进入部分构建状态。',
    solution: '严格校验比例和更新间隔；空分区保持 0，非空分区至少选择一列，并统一两条选择与分配路径。ZeRO-1/2 的小比例分区因此也能保持选择数与缓冲区严格对齐。',
  },
  'openai-agents-sdk': {
    problem: '同名 Agent、Tool 与 MCP Server 以显示名作为键，Graphviz 会合并不同实体并制造伪自环。复杂工作流的节点、边和层级因此被错误折叠，图示不再可信。',
    reasoning: '名称服务于阅读，不等于对象身份；遍历、注册与连边必须共享稳定唯一标识，再保留名称作为标签。身份规则还要跨不同实体类型保持一致，不能只修补某一类节点。',
    solution: '以对象身份注册节点、以原名显示 label，并重构遍历和边生成，使同名实例仍保持独立关系。最终图既保留人类可读名称，也能准确表达真实对象之间的连接。',
  },
  peft: {
    problem: '重复 `adapter_name` 会静默覆盖配置并再次注入层，让已有训练权重处于不可预测的部分更新状态。调用表面成功，模型内部却可能同时混入新旧 Adapter 结构。',
    reasoning: '这是状态原子性边界：若检查发生在突变之后，即使抛出错误，也无法保证模型安全回退。正确的失败路径必须在任何层注入、配置写入或权重变化之前结束。',
    solution: '把唯一性校验前移到所有模型和配置修改之前，冲突立即失败，并确认既有 Adapter 保持原样。危险的静默覆盖由此变成清晰、可恢复且不会破坏训练成果的早期错误。',
  },
  'ultralytics-yolo': {
    problem: '非等比例 resize 与 LetterBox 组合后仍使用单一 gain 还原坐标，预测框无法准确回到原图。输入长宽比差异越大，检测框、掩码和关键点的轴向漂移越明显。',
    reasoning: 'x、y 轴经历了不同缩放与留白；逆变换必须沿完整预处理链分别恢复每个轴。只记录最终统一比例会丢失前置 resize 已经产生的非对称几何信息。',
    solution: '组合前置 resize 与 LetterBox 参数，按双轴比例和 padding 逐轴还原检测、分割等任务坐标。完整变换链被保留下来后，各类视觉输出都能稳定映射回原始图像。',
  },
  timm: {
    problem: 'CutMix minmax 的随机上界使用排除语义，最右侧和最下方的合法裁剪起点永远不会被采到。增强过程看似正常，边缘区域却在每个 epoch 中持续欠采样。',
    reasoning: '这类 off-by-one 不会直接报错，却会在长期训练中形成稳定的空间分布偏差，削弱边界覆盖。离散区间的合法端点必须与裁剪尺寸共同推导，不能照搬连续区间习惯。',
    solution: '修正离散采样上界并补齐边界回归，让所有合法起点重新进入同一均匀分布。右侧与底部端点都能被稳定命中，增强行为重新符合完整空间覆盖的设计语义。',
  },
  supervision: {
    problem: '大坐标多边形计算质心时同时遭遇 int32 溢出和浮点消减，结果会明显漂移甚至失真。大图像、地理坐标或远离原点的几何输入都可能触发这一数值边界。',
    reasoning: '面积矩包含大坐标乘积，仅提升类型仍不足；把几何平移到局部原点才能改善数值条件。局部计算减少大基数相减，再用平移量恢复全局位置，同时保留原有几何定义。',
    solution: '统一转换为 float64，在局部坐标系计算面积与质心，再映射回全局。这样同时缩小坐标乘积与相减基数，既避开整数溢出，也降低浮点消减；普通小坐标输入继续保持原有结果。',
  },
  funasr: {
    problem: '已知说话人数的大输入仍进入谱聚类，构造 O(N²) 相似矩阵，长音频会产生显著内存压力。片段规模增长后，内存开销会先于真正的聚类计算成为部署瓶颈。',
    reasoning: 'K 已知时无需再次估计簇数，应按先验直接选择线性内存、可重复的固定 K 聚类路径。路由条件应同时考虑输入规模和已知先验，而不是让所有场景共享最重算法。',
    solution: '新增特征归一化与确定性 K-means 分支，让大规模输入绕开稠密矩阵和谱分解。固定初始化保证重复运行结果一致，小规模或未知 K 场景仍保留原谱聚类能力。',
  },
  burn: {
    problem: '卷积快速路径对 Inf / NaN 权重的行为与通用实现不同，破坏同一算子的 IEEE 754 语义。用户仅仅切换执行路径，就可能得到不同的非有限值传播结果。',
    reasoning: '异常值需要正确传播，但不能让所有正常输入回退到慢路径；边界应只影响非有限权重。修复必须把语义判断放在足够窄的位置，避免牺牲常规模型吞吐和向量化收益。',
    solution: '检测异常权重并切换到语义一致路径，有限权重继续走原向量化热路径，兼顾正确性与性能。快速与通用实现因此重新共享同一数值约定，正常输入没有性能回退。',
  },
  nltk: {
    problem: '`Concept` 的三个可变默认容器跨实例共享，修改一个对象会悄悄污染之后创建的概念。标签、闭包与扩展集合都会跨对象泄漏，错误状态还会随构造顺序变化。',
    reasoning: 'Python 默认参数只在函数定义时创建一次；默认实例要隔离，显式传入的容器仍应保留调用者语义。修复不能粗暴复制所有输入，否则会改变已有调用方对共享容器的明确选择。',
    solution: '改用 `None` 哨兵按实例创建 list / set，并以双实例回归覆盖全部默认容器的状态隔离。只有缺省路径生成新容器，显式传入对象继续保持原有引用语义。',
  },
  'apache-tvm': {
    problem: 'Relax ONNX Reshape 混淆零复制与字面零维，默认模式和 `allowzero=1` 都可能得到错误形状。规则叠加后，同一个 `0` 会在不同路径中被解释成两种含义。',
    reasoning: '`0` 的含义受 allowzero 控制，而 `−1` 仍负责推断；不同语义不能被同一特殊分支覆盖。只有目标形状确实包含字面零时才需要绕过默认复制规则。',
    solution: '默认先展开零复制；仅在允许且确有字面零时构造 ShapeExpr，其余路径继续保留正常 `−1` 推断。三条分支分别覆盖维度复制、真实零维输出与动态维度推断。',
  },
  torchvision: {
    problem: 'JPEG v2 用 `view()` 展平批次维，transpose 产生的合法非连续 Tensor 会在编码前直接报错。接口明明支持任意前导批次维，却暗中要求调用者先手动整理内存布局。',
    reasoning: '逻辑形状与内存连续性不是一回事；接口应保留元素顺序，同时只在确有必要时复制数据。`reshape()` 能在兼容布局上继续返回视图，在其他情况下安全物化副本。',
    solution: '将展平改为 `reshape()` 并恢复原形状，以转置批次轴对照连续输入，锁定多前导维场景。非连续 Tensor 可以直接编码，已有连续输入仍保留零额外复制的快速路径。',
  },
  lerobot: {
    problem: '`repo_ids` 与 `roots` 数量不一致时，普通 `zip` 会静默丢弃尾部数据集，任务却继续完成。最终产物看起来合法，实际来源数量和训练数据内容已经悄悄减少。',
    reasoning: '两组列表共同描述同一批数据源，长度相等是公开 API 的前置不变量，必须在读取数据前验证。越早失败，越能避免部分元信息加载掩盖真正的输入错误。',
    solution: '入口先校验数量并给出清晰错误，再使用 strict pairing，阻止机器人训练数据被静默截断。合法调用保持原有聚合行为，非法请求在触碰任何数据集之前结束。',
  },
};
